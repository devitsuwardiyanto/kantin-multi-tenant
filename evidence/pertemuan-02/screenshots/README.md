# Screenshots — Pertemuan 02

- `customer /kantin/demo`: layout pelanggan mobile-first (max-w-md), header "Kantin: demo",
  komponen x-empty-state "Katalog belum tersedia (Modul 7)", footer. Diverifikasi live (HTTP 200), tema Flux gelap.
- Layout tenant (`/tenant/demo/dashboard`) & admin (`/admin/dashboard`): terverifikasi merender via feature
  test (assertOk = view terkompilasi tanpa exception). Demo manual dengan akun pada demo-script.md.

PNG binari tidak disimpan (menghindari bloat); render dikonfirmasi live/test.
