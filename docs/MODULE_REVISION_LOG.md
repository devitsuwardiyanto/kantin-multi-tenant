# Module Revision Log

Revisi dokumen modul hanya dilakukan bila ada bukti (lihat protokol revisi pada prompt tugas).
Base dokumen aktual: `Modul_..._v7.docx`. Target revisi kumulatif: **`Modul_..._v8.docx`** (dikonfirmasi pemilik 2026-08-16, **sudah dibuat** di root repo).

Status: `DIUSULKAN` (baru catatan) · `DITERAPKAN` (kode teruji + DOCX diperbarui + inspeksi visual).

| ID | Modul/Tahap | Temuan | Bukti | Resolusi | Status |
|---|---|---|---|---|---|
| DOC-01-001 | Global / parameter | Prompt menyebut base `v4` → revisi `v5`, tetapi berkas nyata di `references/` adalah **v7** dan tidak ada v4/v5. | `ls references/` hanya memuat `..._v7.docx`. | **DISEPAKATI**: base **v7**, revisi kumulatif ditulis ke `..._v8.docx` (konfirmasi pemilik 2026-08-16). | DITERIMA |
| DOC-01-006 | Modul 1 / Tahap 5 | `php artisan install:broadcasting` diasumsikan menyelesaikan setup realtime, tetapi langkah build npm-nya gagal di lingkungan non-interaktif (TTY) dan tidak menuliskan `REVERB_*` env maupun memasang `laravel-echo`/`pusher-js`. | Output installer: `TTY mode requires /dev/tty`; `npm run build` gagal resolve `laravel-echo`; `.env` tanpa `REVERB_*` → `package:discover` error broadcaster reverb. | Tambah langkah eksplisit: `npm install laravel-echo pusher-js` dan isi `REVERB_APP_ID/KEY/SECRET` di `.env`/`.env.example`. Terverifikasi (`package:discover` bersih, build OK). Perbarui Tahap 5 & troubleshooting. | DITERAPKAN (v8) |
| DOC-01-002 | Global / basis desain | Modul menyatakan basis "SRS v1.1", input tersedia adalah **SRS v2**. | Header modul baris "Basis desain: SRS v1.1"; berkas `SRS_..._ISO29148 v2.docx`. | Audit diff SRS v1.1→v2 sebelum Modul 3 (data). Perbarui RTM & ketertelusuran ke SRS v2 (otoritas lebih tinggi). | DITERAPKAN (v8) |
| DOC-01-003 | Global / strategi Git | Modul: `main → develop → feat/week-NN`, evidence minimal, tanpa PR/tag/release. Prompt (otoritas lebih tinggi): `main` + `feat/pertemuan-NN-slug` + annotated tag `pertemuan-NN` + GitHub Release + CI required checks. | Bagian "Strategi Branch dan Evidence" modul vs kontrak GitHub pada prompt. | Ganti strategi ke model prompt; Tahap 7 tiap modul mencakup PR→CI→merge→tag→release; `git worktree` untuk membuka versi berdampingan. | DITERAPKAN (v8) |
| DOC-01-004 | Modul 1 / Tahap 4 & cuplikan | Modul memakai `DB_PORT=3307`, `DB_DATABASE=kantin_multi_tenant`, `REDIS` port `6380`. Environment nyata (kredensial pengguna, terverifikasi live): `DB_PORT=3306`, `DB_DATABASE=kantin`, `DB_USERNAME=kantin`, Redis `6379`. | `mysql -h127.0.0.1 -P3306 -ukantin` → MariaDB 11.4.5, db `kantin` ada; `redis-cli -p 6379 ping` → PONG; `-p 6380` connection refused. | Gunakan konfigurasi nyata pengguna (otoritas tertinggi). Lihat [ADR-0002](adr/0002-database-redis-connection.md). Perbarui Tahap 4, cuplikan `.env.example`, tabel troubleshooting port. | DITERAPKAN (v8) |
| DOC-01-005 | Modul 1 / intro vs Tahap 4 | Zona waktu tidak konsisten: intro menyebut `Asia/Jakarta`, Tahap 4 menyebut `Asia/Makassar`. | Baris intro §mode produksi vs Tahap 4. | Simpan UTC di DB; presentasi `Asia/Jakarta` (WIB). Lihat [ADR-0003](adr/0003-timezone.md). Seragamkan teks modul. | DITERAPKAN (v8) |
| DOC-01-007 | Modul 1 / Tahap 1–7 (Lakukan), Prasyarat, Rencana Waktu, Landasan Teknis, Guided Lab | Instruksi setup tidak menyebut perintah/klik yang harus dilakukan; merujuk `check-environment.sh` yang tidak ada di repo dan Docker; perintah bash (`./vendor/bin/pint`); prompt interaktif `laravel new` (fitur autentikasi) dan `install:broadcasting` (dua Yes/No) tidak dijelaskan; `.env.example` tidak mendapat variabel `REVERB_*` dari `install:broadcasting`. | Langkah dijalankan di folder bersih: Laravel Installer 5.32 → `laravel new … --livewire --phpunit --database=mysql --npm --no-boost` (Laravel 13.33, Livewire 4.4), konfigurasi MariaDB 11.4 + Redis, Reverb, `php artisan test` 34 passed, Pint PASS 48 files, `npm run build`, commit tanpa `.env`; `.env.example` hasil langkah identik dengan `pertemuan-01`; prompt diamati lewat pseudo-terminal. | Stack Windows = ServBay (PHP 8.3, MariaDB, Redis, Node 24 — Node < 22.18 memicu EBADENGINE); setiap instruksi diikuti perintah cmd dengan output nyata (`::`), blok SQL monitor MariaDB (database `kantin`, `kantin_test`, user `kantin`), tabel perubahan `.env`/`.env.example`, penjelasan prompt dan baris wajar “✘ Database migrated”, PATH Composer global lewat klik; teks `check-environment.sh`/Docker diganti; Landasan Teknis & Guided Lab → cmd. | DITERAPKAN (v8) |

| DOC-02-002 | Modul 2 / Tahap 2 & Landasan Teknis | Tahap 2 hanya meminta `app/Modules/{Module}/{Actions,Data,Services}`, sedangkan Landasan Teknis mencontohkan provider yang memuat route dari folder modul (`loadRoutesFrom(app/Modules/Orders/routes/web.php)`) tanpa pengaman middleware portal. Akibatnya controller/view/Livewire fitur tersebar di `app/Http`, `resources/views`, `routes` (modul hanya berisi service) dan route modul berisiko tanpa `auth`/`role`. | Implementasi v1 P2–P14: fitur Kitchen tersebar di 6 lokasi; provider modul kosong. | Konvensi vertical slice: base `App\Modules\ModuleServiceProvider` (route/view/Livewire per modul, alias huruf kecil) + `App\Support\Routing\PortalRoutes` (middleware portal tunggal). Model/UI shared tetap standar Laravel. Diuji `ModuleConventionTest` (modul tiruan Probe). Revisi Tahap 2 + Landasan Teknis Modul 2 di v8. | DITERAPKAN (v8) |
| DOC-02-003 | Modul 2 / Tahap 1–7 | Tahapan hanya menyebut "Mari kita buat …" tanpa perintah pembuatan berkas; peserta tidak tahu generator Artisan mana yang dipakai, di mana berkas tersimpan, dan jebakan seperti `make:provider` yang otomatis menulis ulang `bootstrap/providers.php`. | Seluruh perintah dijalankan di worktree bersih (Laravel 13.25): 29 perintah menghasilkan tepat 77 berkas baru Pertemuan 2. | Tambah blok BUAT FILE per tahap (perintah Artisan/manual + fungsi + berkas hasil & direktori + output INFO nyata) di v8. | DITERAPKAN (v8) |
| DOC-02-001 | Modul 2 / Tahap 3 & cuplikan | Modul memakai `{tenant:slug}`/`{canteen:slug}` + `scopeBindings()`, tetapi model Tenant/Canteen baru dibuat pada Modul 3 dan isolasi/binding pada Modul 4 — jika ditulis di Modul 2, implicit binding gagal (model belum ada). | Route group tenant/customer dibuat di Modul 2 dengan parameter string; `php artisan route:list` menampilkan `tenant/{tenant}`, `kantin/{canteen}` tanpa error. | Modul 2 membangun kerangka route (nama+middleware+placeholder); `scopeBindings` + model binding di-wire pada Modul 4. Perbarui teks Tahap 3 agar eksplisit menyatakan binding ditunda. | DIUSULKAN |
| DOC-02-004 | Modul 2 / Tahap 1–7 (Lakukan) | Tahap 1 hanya membuat `docs/adr/0006-modular-monolith.md`; instruksi (“Mari kita petakan model dan use case ke enam modul.”) tidak menyebut apa yang dikerjakan; perintah memakai sintaks bash (`mkdir -p {a,b}`, `find … -exec`, `touch`, kutip tunggal, `./vendor/bin/pint`) yang tidak berjalan di cmd Windows; nama berkas `⚡counter.blade.php` sulit diketik di cmd; `bootstrap/app.php` dan `bootstrap/providers.php` tidak dijelaskan. | Verifier menjalankan 22 langkah (cmd diterjemahkan ke bash; perintah `for` memakai padanan bash) di worktree bersih dari `pertemuan-01`: anchor unik, `php -l` + `route:list` lulus per tahap, hasil Tahap 7 identik dengan `pertemuan-02` (80ef839), 49 test passed. | Setiap instruksi diikuti blok cmd (`for %m in (…) do mkdir`, `for /d /r … type nul > .gitkeep`, `type nul >`, `ren`, kutip ganda) dengan output nyata sebagai baris `::`, lalu isi berkas lengkap atau sisipan “cari … tambahkan”; Tahap 1 tanpa ADR (cek `php artisan about`, peta enam modul → use case, aturan event lintas modul); berkas ⚡ dibuat lewat klik VS Code; alias `role` baru didaftarkan setelah middleware dibuat (Tahap 5); callout Persiapan Windows; Guided Lab bash → cmd. | DITERAPKAN (v8) |

| DOC-03-001 | Modul 3 / prasyarat & Tahap 1 | Modul mensyaratkan "SQL baseline Fase 0" pada lampiran `00_database`, tetapi berkas itu TIDAK ada di `references/` (hanya `ERD_...svg`). | `ls references/` tanpa SQL. | Skema diturunkan dari ERD SVG + invariant modul; dicatat sebagai sumber. Bila SQL baseline nanti tersedia, audit deviasi. | DIUSULKAN |
| DOC-03-002 | Modul 3 / Tahap 3 | Modul menyebut token hash `BINARY(32)`. Laravel `$table->binary('col',32)` menghasilkan `varbinary(32)`. | `information_schema` → `varbinary(32)`. | Setara fungsional untuk menyimpan hash 32-byte + UNIQUE; dipertahankan. Perbarui teks agar menyebut varbinary(32). | DIUSULKAN |
| DOC-03-003 | Modul 3 / cuplikan | Cuplikan modul memakai tanggal migrasi `2026_07_19_...`. Implementasi memakai penomoran `2026_08_16_0001NN_create_*` sesuai tanggal kerja nyata dan urutan domain. | Nama file migrasi repo. | Selaraskan indeks source & cuplikan ke penamaan aktual. | DIUSULKAN |
| DOC-03-004 | Modul 3 / Tahap 2–7 | Tahapan migrasi/model/seeder tidak menyebut generator: nama migrasi, opsi `--factory`, penamaan ulang timestamp agar urutan FK deterministik, dan cara membuat schema dump belum dijelaskan. | 25 perintah dijalankan di worktree (state akhir P2) menghasilkan 39 berkas baru P3; berkas ke-40 (`schema-dump.sql`) berasal dari `mariadb-dump --no-data`. | Tambah blok BUAT FILE per tahap (make:migration ×6, make:model ±--factory ×14, make:seeder, make:test, mariadb-dump) di v8. | DITERAPKAN (v8) |
| DOC-03-005 | Modul 3 / Tahap 1–7 (Lakukan), Prasyarat, Guided Lab | Tahap 1 hanya berisi daftar niat (“kelompokkan 30 tabel”); instruksi tidak menyebut isi berkas; perintah bash (`mv`, `mkdir -p {…}`, `touch`, `./vendor/bin/pint`); Tahap 7 membuat `docs/adr/0007-baseline-schema.md`; prasyarat merujuk lampiran `00_database`. | 20 langkah dijalankan di worktree bersih dari `pertemuan-02` (cmd → bash): anchor unik, `php -l` + `route:list` lulus per tahap, hasil Tahap 7 identik dengan `pertemuan-03` (46a16a6), 55 test passed; EXPLAIN dijalankan nyata di MariaDB (`type: ref`, key index komposit). | Tahap 1 tanpa ADR: enam kelompok migrasi, platform-scoped vs tenant-owned, target composite unique; setiap migrasi `make:migration` → `ren … 2026_08_16_0001NN_…` → isi berkas; 14 model + 8 factory + seeder dengan isi lengkap; `php artisan db` + blok SQL EXPLAIN; `mariadb-dump` interaktif; data dictionary; Guided Lab → cmd. | DITERAPKAN (v8) |

| DOC-04-001 | Modul 4 / Tahap 3 | Guard tenant Modul 2 memakai kolom `role` (coarse). Modul 4 menjadikan route tenant sumber tenant aktif: bind `{tenant:slug}`, cek membership `user_tenant_roles` + status, isi TenantContext. | Middleware `SetTenantContext`; grup `PortalRoutes::tenant()` → `tenant/{tenant:slug}` + `scopeBindings()` (berlaku juga untuk route modul, mis. `app/Modules/Catalog/routes/tenant.php`). | Ganti `role:tenant` dengan resolver berbasis membership; perbarui tes fondasi Modul 2 (tenant nyata + membership). Selaraskan teks Tahap 3. | DIUSULKAN |
| DOC-04-002 | Modul 4 / Tahap 4 | Cuplikan modul menerapkan global scope hanya saat `context->has()` (fail-open tanpa context). | `BelongsToTenant::bootBelongsToTenant`. | Dipertahankan agar find/refresh/seed tanpa context berfungsi; dimitigasi: route internal selalu mengisi context, bypass eksplisit untuk publik, write tanpa context gagal via NOT NULL tenant_id. Dokumentasikan mitigasi di ADR-0008. | DIUSULKAN |
| DOC-04-003 | Modul 4 / Tahap 3 & 5 | Teks memasang alias middleware dan `scopeBindings()` langsung di grup route `bootstrap/app.php`; dengan modul vertical slice, route fitur berada di `app/Modules/*/routes` sehingga pengaman harus melekat pada grup portal. | `route:list --path=tenant`: `tenant.menus.*` dilayani `App\Modules\Catalog\Http\Controllers\TenantMenuController` dengan `{tenant:slug}` + `tenant` + scoped bindings; `ModuleConventionTest` + `TenantIsolationTest` hijau (65 test). | `PortalRoutes::tenant()` memakai `tenant/{tenant:slug}` + `scopeBindings()` + `tenant`; `TenantMenuController` di modul Catalog. Revisi Tahap 3 & 5 + Output Nyata Modul 4 (Gambar M4b) di v8. | DITERAPKAN (v8) |
| DOC-04-004 | Modul 4 / Tahap 2–7 | Tahapan tidak menyebut generator; `make:trait Models/Concerns/BelongsToTenant` justru menulis ke `app/Concerns/Models/Concerns/` (namespace bawaan `App\\Concerns`), dan controller/route modul Catalog perlu nama kelas lengkap. | 12 perintah dijalankan di worktree (state akhir P3) menghasilkan tepat 18 berkas baru P4; jebakan make:trait direproduksi. | Tambah blok BUAT FILE per tahap (make:class/job/middleware/trait FQN/policy --model/controller FQN/test + route modul manual) di v8. | DITERAPKAN (v8) |
| DOC-04-005 | Modul 4 / Tahap 1–7 (Lakukan), Guided Lab | Instruksi tidak menyebut isi berkas; perubahan `bootstrap/app.php`, `PortalRoutes`, `AppServiceProvider`, route/view dashboard, dan test lama (`role:tenant` → `tenant`) tidak dijelaskan; perintah bash (`rm`, `touch`, kutip tunggal pada `make:trait`); Tahap 7 membuat `docs/adr/0008-tenant-isolation.md`. | 23 langkah dijalankan di worktree bersih dari `pertemuan-03`: anchor unik, `php -l` + `route:list` lulus per tahap, kode hasil Tahap 7 identik dengan `pertemuan-04` (db0e0ab; hanya `docs/architecture.md` berbeda satu kalimat karena rujukan ADR dihapus), 65 test passed. Temuan: `findstr … tenant_id … fillable` menampilkan `UserTenantRole` (pivot keanggotaan, diisi service admin) — dijelaskan di modul. | Tahap 1 tanpa ADR: data wajib scoped, tabel keputusan konteks (SRS 6.3), urutan resolver, jalur bypass; test lama diperbarui di Tahap 3 dan asersi `scopeBindings` di Tahap 5 agar suite tetap hijau per tahap; pemasangan trait pada delapan model sebagai satu pola perubahan; audit `findstr withoutGlobalScope`; Guided Lab → cmd. | DITERAPKAN (v8) |

| DOC-08-001 | Global / semua modul (v8) | Permintaan pemilik: (a) waktu efektif di kelas 3 jam 30 menit/pertemuan + tugas mandiri ≤2 jam untuk pekerjaan sejenis (mengulang); (b) gabungkan bagian "Pengetahuan Teknis" dan "Penjelasan Mandiri"; (c) revisi & screenshot output nyata LANGSUNG di bagian modul, bukan lampiran. | Instruksi pemilik 2026-08-17. | Diterapkan pada v8: Durasi & Rencana Waktu jadi 210' kelas (7×30') + catatan mandiri 120' dengan contoh CRUD 2→3 tabel; dua section pengetahuan digabung (heading kedua dihapus, body mengalir); subbagian "Output Nyata Terverifikasi" + tangkapan layar nyata disisipkan in-body Modul 1–4; Lampiran G diringkas jadi change-log (output dipindah ke modul). Cover 84→49 jam. | DITERAPKAN (v8) |
| DOC-08-004 | Modul 8 / Landasan Teknis, Cuplikan, Output Nyata | Path `app/Services/CartService.php`, `08_cart/...` di luar modul; komponen keranjang di `resources/views/components`; blok Output Nyata Modul 8 v1 tersisip DI DALAM `<w:pPr>` judul Modul 9 (paragraf bersarang, skema OOXML tidak valid — pola sama pada P9–P14 v1, total 21 di `main`). | `find app/Modules/Ordering`; uji keranjang 16 + suite 109 hijau; pemeriksaan nesting DOCX: v1 `main` 21 → v2 0. | Keranjang di modul Ordering (`ordering::cart`); path diselaraskan; fungsi `repair` memindahkan blok ke badan modul pemiliknya sebelum "Observe" + Output Nyata M8b. | DITERAPKAN (v8) |
| DOC-08-005 | Modul 8 / Tahap 1–7 | Tahapan tidak menyebut generator/lokasi berkas. | 11 perintah dijalankan di worktree (state akhir P7) menghasilkan 12 dari 13 berkas baru P8 (sisanya screenshot). | Blok BUAT FILE per tahap (make:class DTO/service, make:exception FQN, make:livewire ordering::cart, make:test). | DITERAPKAN (v8) |

| DOC-05-001 | Modul 5 / Tahap 4 | Modul menyebut "check/trigger database" sebagai lapis terakhir anti-overlap komisi. | Implementasi memakai generated column `active_lock` + UNIQUE (satu skema valid_to NULL per tenant). | Setara & portabel di MariaDB tanpa trigger; menegakkan satu skema aktif/tenant. Overlap penuh dicegah service (lock+tutup+insert). Terbukti 1062. Sesuaikan teks Tahap 4. | DITERAPKAN (kode) |
| DOC-05-002 | Modul 5 / cuplikan | Cuplikan `ChangeCommission` memakai `Carbon`. Aplikasi memakai `Date::use(CarbonImmutable)`. | Service memakai `CarbonInterface` agar menerima now()/immutable. | Sesuaikan tipe parameter di cuplikan modul. | DITERAPKAN (kode) |
| DOC-05-003 | Modul 5 / Tahap 5 | Guard "satu primary account" ditegakkan generated column `primary_lock` + UNIQUE. | information_schema uq_bank_primary_per_tenant. | Tambahkan catatan guard DB primary di teks Tahap 5. | DITERAPKAN (kode) |
| DOC-05-004 | Modul 5 / Landasan Teknis, Contoh Mandiri, Cuplikan | Path contoh (`app/Http/Requests/StoreTenantRequest.php`, `app/Services/ChangeCommission.php`, `05_admin/...`) berada di luar modul, bertentangan dengan konvensi vertical slice. | `route:list --path=admin/tenants`: 10 route `admin.tenants.*` → `App\Modules\Admin\Http\Controllers\*`; `AdminManagementTest` 9 passed; suite 74 passed. | Controller/Form Request/view/route administrasi di `app/Modules/Admin`; path di DOCX diselaraskan + Output Nyata Modul 5 (Gambar M5b). | DITERAPKAN (v8) |
| DOC-05-005 | Modul 5 / Tahap 1–7 | Tahapan tidak menyebut generator; `make:view admin::tenants.index` terbukti membuat folder `resources/views/admin::tenants/` (namespace modul tidak dikenali). | 21 perintah dijalankan di worktree (state akhir P4) menghasilkan 29 dari 30 berkas baru P5 (berkas ke-30 = screenshot evidence). | Blok BUAT FILE per tahap: make:request/controller FQN ke modul Admin, make:class, make:policy, make:model, make:migration, make:test; view/route modul manual. | DITERAPKAN (v8) |
| DOC-05-006 | Modul 5 / Konsep & Contoh Mandiri komisi | Versi lama ditutup `valid_to = effectiveAt − 1 detik` sementara pembaca skema memakai `valid_to > now` (akhir eksklusif). Dengan timestamp mikrodetik, rentang `[effectiveAt − 1s, effectiveAt)` tidak memiliki skema berlaku → checkout gagal `commissionNotConfigured` tepat di jendela pergantian. | `CommissionScheduleBoundaryTest`: dengan `subSecond()` 2/3 test gagal (tidak ada skema pada T−1s); dengan `valid_to = effectiveAt` 3/3 lulus. | Interval setengah-terbuka `[valid_from, valid_to)`: `ChangeCommissionSchedule` menutup versi lama tepat di `effectiveAt`; scope `CommissionScheme::effectiveAt()` sebagai definisi tunggal skema berlaku (dipakai checkout Modul 9). Konsep, Contoh Mandiri, dan blok BUAT FILE Tahap 6 Modul 5 diperbarui di v8. | DITERAPKAN (v8) |
| DOC-05-007 | Modul 5 / Use case SRS v2 (rantai v3) | Modul 5 membangun administrasi tenant tetapi belum menuntaskan UC-19 (penguncian 5x/10 menit, pesan generik, redirect per peran, sesi 8 jam) dan UC-21 (onboarding penanggung jawab + rekening + kredensial awal, penonaktifan tenant, riwayat komisi beserta pengelola). | `LoginLockoutTest` 10 + `TenantOnboardingTest` 6 passed; suite 93 passed; uji browser: percobaan ke-6 terkunci, onboarding → tautan atur sandi → owner masuk ke dasbor tenantnya, Kopi Kita nonaktif. | Tambah bagian Use Case SRS (tabel UC + mockup #uc-19/#uc-21), Tahap 8 + blok BUAT FILE, output nyata browser, dan catatan rencana waktu di Modul 5. | DITERAPKAN (v8) |
| DOC-05-008 | Modul 5 / Tahap 1–8 (Lakukan) | Instruksi seperti “Mari kita buat state transition tenant.” tidak menyebut apa yang harus diketik; Tahap 1 hanya membuat `docs/adr/0009-admin-commission-audit.md`; perintah memakai sintaks bash (`touch`, `mkdir -p`, `{a,b}`, `./vendor/bin/pint`, kutip tunggal) yang gagal di cmd Windows. Urutan v2 juga memakai `AuditLogger`/`UserCanteenRole` di Tahap 2 padahal baru dibuat di Tahap 3/5, dan `php artisan db:seed` gagal 1062 pada database yang sudah di-seed. | Verifier menjalankan 32 langkah (cmd diterjemahkan ke bash) di worktree bersih dari `pertemuan-04`: setiap anchor unik, `php -l` + `route:list` lulus per tahap, hasil akhir Tahap 7 identik dengan 59f46b1 dan Tahap 8 identik dengan dcb302f, 93 test passed. | Setiap instruksi kini diikuti blok perintah cmd (kutip ganda, `type nul >`, `mkdir a\b`, `del`, `ren`) dengan output nyata sebagai baris `::`, lalu isi berkas lengkap atau sisipan “cari … tambahkan di bawah/atas”; Tahap 1 merancang transisi status + membuat UserCanteenRole, TenantPolicy, AuditLog, AuditLogger (tanpa ADR); seeder memakai `--class=DemoCanteenSeeder`; callout Persiapan Windows; blok Guided Lab bash → cmd. | DITERAPKAN (v8) |
| DOC-00-001 | Seluruh modul / blok kode | Blok kode berlatar hitam (palet Visual Studio Code Dark+, latar 1E1E1E, label 2D2D30) sulit dibaca saat dicetak dan tidak sesuai permintaan tema. | 208 blok kode + 133 label dirender ulang; teks setiap blok identik karakter-per-karakter dengan sebelumnya; pipeline (langkah → tema) idempoten; tidak tersisa warna 1E1E1E/2D2D30/D4D4D4. | Latar terang Cyan Light Theme (JetBrains, OlyaB/CyanTheme `cyanScheme.xml`: latar f2f3f7, teks 262626, keyword 000080, string 00733b, angka/atribut 0073e6, komentar 808080, variable/properti 660e7a) dengan kategori token standar VS Code untuk PHP, HTML/Blade, cmd, bash (fungsi 795e26 dan tipe 267f99 dari VS Code); penjelasan palet di “Cara Membaca Sampel Kode” dan Lampiran diperbarui. | DITERAPKAN (v8) |

| DOC-06-001 | Modul 6 / Tahap 4 & prasyarat | Modul memakai `chillerlan/php-qrcode` untuk raster QR. | Pedoman Boost: jangan menambah dependency tanpa persetujuan; modul menyatakan keamanan token tidak bergantung library gambar. | Raster QR ditunda; halaman QR menampilkan URL sekali-tampil. Tambahkan dependency setelah disetujui, arahkan ke URL. | DIUSULKAN |
| DOC-06-002 | Modul 6 / model | Cast `datetime` pada model tidak terbaca larastan tanpa `@property`. | PHPStan error isFuture() on string. | Tambah docblock `@property CarbonImmutable|null` pada model bertanggal (konvensi seperti User). | DITERAPKAN (kode) |
| DOC-06-003 | Modul 6 / Tahap 5 | Cuplikan memakai `app/Domain/...`. Repo memakai `app/Support/Tokens`, `app/Modules/{Admin,Ordering}/Services`. | Struktur repo modular monolith (Modul 2). | Selaraskan path cuplikan ke struktur aktual. | DITERAPKAN (kode) |
| DOC-06-004 | Modul 6 / Landasan Teknis & Cuplikan | Path contoh `app/Domain/Tables/...` dan `06_qr_session/...` tidak ada di repo; controller scan QR & route publik semula di `app/Http` / `routes/web.php`, limiter di `AppServiceProvider`. | `route:list`: `admin.tables.*` → `App\Modules\Admin\Http\Controllers\AdminDiningTableController`; `customer.scan` → `App\Modules\Ordering\Http\Controllers\ResolveTableQrController`; 8 uji QR/token + suite 85 hijau. | Meja di modul Admin; scan QR + route publik (`PortalRoutes::web()`) + limiter `qr-scan` di modul Ordering; path DOCX diselaraskan + Output Nyata M6b. | DITERAPKAN (v8) |
| DOC-06-005 | Modul 6 / Tahap 1–7 | Tahapan tidak menyebut generator/lokasi berkas. | 15 perintah dijalankan di worktree (state sebelum fitur P6) menghasilkan 21 dari 23 berkas baru P6 (sisanya 2 screenshot evidence). | Blok BUAT FILE per tahap (make:model, make:policy, make:controller FQN + --invokable, make:trait FQN, make:class, make:test --unit; view/route modul manual). | DITERAPKAN (v8) |
| DOC-06-006 | Modul 6 / Use case SRS v2 (rantai v3) | Modul 6 belum menuntaskan UC-22 (QR siap cetak, nonaktif/aktif meja) dan UC-02 (halaman sambutan meja + status tenant, pesan QR tidak valid, penolakan di luar jam operasional); halaman QR menyatakan raster QR ditunda. | `TableQrUseCaseTest` 3 + `CustomerScanUseCaseTest` 5 passed; suite 109 passed (array & Redis); uji browser: QR SVG Meja 1 → pindai → “Selamat datang, Meja 1 — Indoor”; token lama → pesan hubungi petugas; jam tutup → “Kantin sedang tutup” tanpa sesi. | Bagian Use Case SRS (UC-22/UC-02 + mockup), Tahap 8, output nyata browser; `bacon/bacon-qr-code` dideklarasikan di composer.json (disetujui pengguna). | DITERAPKAN (v8) |
| DOC-06-007 | Modul 6 / Tahap 1–8 (Lakukan), Prasyarat, Guided Lab | Instruksi tanpa isi berkas; perintah bash (`mkdir -p`, `rm`, `touch`, kutip tunggal); Tahap 1 membuat `docs/adr/0010-…md`. | 21 langkah dijalankan di worktree bersih dari akhir Pertemuan 5 (v3): hasil Tahap 7 identik dengan kode v2 Pertemuan 6 (101 test), hasil Tahap 8 identik dengan kode akhir (109 test). | Format langkah Windows (cmd + VS Code) dengan output nyata `::`, isi berkas lengkap, tanpa ADR; Guided Lab → cmd; tema kode Cyan Light. | DITERAPKAN (v8) |

| DOC-07-001 | Modul 7 / Tahap 2-3 | Modul memakai upload foto + WebP via `intervention/image`. | Pedoman Boost: tanpa menambah dependency tanpa persetujuan; kolom foto belum ada di skema Modul 3. | Foto menu ditunda; katalog fungsional tanpa gambar. Tambahkan dependency + migrasi kolom path setelah disetujui. **Rantai v3:** diselesaikan tanpa dependency baru (GD → WebP, migrasi `photo_path`), lihat DOC-07-006. | DITERAPKAN (v3) |
| DOC-07-002 | Modul 7 / Tahap 5 (Livewire) | Modul tidak menyebut bahwa update Livewire tidak melewati middleware route, sehingga TenantContext hilang pada aksi berikutnya. | Perilaku Livewire 4. | Komponen tenant meng-`booted()` untuk re-verify membership (prop publik tak dipercaya) + set TenantContext tiap request. Tambahkan catatan trap ini. | DITERAPKAN (kode) |
| DOC-07-003 | Modul 7 / cuplikan | Cuplikan memakai `app/Services`, `app/Queries`, komponen kelas. Repo memakai `app/Modules/Catalog/Services`, `PublicCatalogQuery`, dan Livewire SFC (⚡) sesuai starter kit. | Struktur repo + konvensi Livewire proyek. | Selaraskan path & format komponen pada cuplikan. | DITERAPKAN (kode) |
| DOC-07-004 | Modul 7 / Contoh Mandiri, Landasan Teknis, Cuplikan | Path contoh `app/Services/MenuImageService.php`, `app/Queries/PublicMenuCatalog.php`, `07_catalog/...` tidak sesuai struktur modul; komponen Livewire semula di `resources/views/components`. | `find app/Modules/Catalog`: komponen `⚡menu-manager`/`⚡menu-catalog`, halaman, route di modul; route `tenant.menu-manager` → `app/Modules/Catalog/routes/tenant.php`; 8 uji katalog + suite 93 hijau. | Komponen `catalog::…` (dibuat `make:livewire catalog::…`), halaman `catalog::tenant.menu-manager`, route di modul Catalog; path DOCX diselaraskan + Output Nyata M7b. | DITERAPKAN (v8) |
| DOC-07-005 | Modul 7 / Tahap 1–7 | Tahapan tidak menyebut generator/lokasi berkas; `make:livewire` tanpa namespace menaruh komponen di `resources/views/components`. | 8 perintah dijalankan di worktree (state akhir P6) menghasilkan 13 dari 15 berkas baru P7 (sisanya 2 screenshot); `make:livewire catalog::menu-manager` terbukti menulis ke `app/Modules/Catalog/resources/views/livewire`. | Blok BUAT FILE per tahap (make:livewire catalog::…, make:class, make:test; halaman/route modul manual). | DITERAPKAN (v8) |
| DOC-07-006 | Modul 7 / Use case SRS v2 (rantai v3) | Modul 7 belum menuntaskan UC-13 (ubah/hapus menu, deskripsi, foto, cari/saring kategori), UC-14 (tandai habis satu klik), UC-01 (menu per tenant, menu habis tampil nonaktif, tenant tutup + jam buka, chip kategori, pembaruan berkala) dan UC-11 (estimasi waktu tunggu); foto menu ditunda (DOC-07-001). | `MenuManagerUseCaseTest` 4 + `CatalogBrowseUseCaseTest` 3 passed; `CatalogPublicTest` diselaraskan dengan SRS alur 4a; suite 124 passed (array & Redis); uji browser: unggah foto → WebP tampil, sakelar → HABIS, katalog `/kantin/kantin-pusat` per tenant + “Antrean ± … mnt”. | Bagian Use Case SRS (UC-13/14/01/11 + mockup), Tahap 8, output nyata browser; foto via GD bawaan PHP (tanpa dependency baru) → DOC-07-001 selesai. | DITERAPKAN (v8) |
| DOC-07-007 | Modul 7 / Tahap 1–8 (Lakukan), Prasyarat, Guided Lab | Instruksi tanpa isi berkas; Tahap 1 membuat `docs/adr/0011-…md`; Tahap 3/6 (CRUD modifier, dialog detail) tanpa kode pendukung. | 19 langkah dijalankan di worktree bersih dari akhir Pertemuan 6 (v3): hasil Tahap 7 identik dengan kode v2 Pertemuan 7 (117 test), hasil Tahap 8 identik dengan kode akhir (124 test). | Format langkah Windows (cmd + VS Code), isi berkas lengkap, tanpa ADR; Tahap 3/6 dinyatakan sebagai rancangan yang diimplementasikan bersama UC-04 (Pertemuan 8); tema kode Cyan Light. | DITERAPKAN (v8) |

> Catatan: edit DOCX v8 **sudah diterapkan** (textual revisions + Lampiran G change-log & output terminal terverifikasi Modul 1). Inspeksi visual: halaman sampul diverifikasi via Quick Look (SRS v2, Versi 8.0); render penuh per halaman `TIDAK DIUJI` (LibreOffice/Word-automation tidak tersedia di environment ini) — buka v8 di Word untuk verifikasi akhir. Output terminal Modul 2–14 ditambahkan bertahap saat modul diimplementasikan (tanpa mengarang).
